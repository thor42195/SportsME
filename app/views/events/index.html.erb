<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<head>
  <meta charset="UTF-8">
</head>
<body>
	<%= render"layouts/user-header"%>
	<p><%= notice %></p>
	<div class="container">
		<div class="row">
			<% @events.reverse_each do |event| %>
			<div class="col-xs-12 col-md-6 picture-panel"></div>
		</div>
		<div class="card" style="padding:10px">
      <div class="card-header">
				イベント名：<%= event.title%>
      </div>
			<div class="card-header">
				イベント主催者:<%= event.organizer.name%>
				<!--イベント主催者のプロフィールページに飛べるようにする-->
				<%#= link_to 'プロフィール', organizers_path(id: event.organizer.id), class: 'btn btn-info' %>
      </div>
			<div class="card-header">
				場所：<%= event.place%>
      </div>
			<div class="card-header">
				日程：<%= event.starttime.strftime("%Y-%m-%d %H:%M:%S")%>
	        ~ <%= event.endtime.strftime("%Y-%m-%d %H:%M:%S")%>
					（申し込み期限:<%= event.deadline.strftime("%Y-%m-%d %H:%M:%S")%>）
      </div>
			<div class="card-header">
				<% if event.paticipants.count < event.number %>
          参加人数：<%= event.paticipants.count %>/<%= event.number %>
        <% else %>
          定員数に達したので、参加できません。（参加人数：<%= event.number %>）
        <% end%>
      </div>
			<% if event.image %>
				  <%= image_tag(event.image, :class =>"card-image")%>
		 <% end %>
		 <div class="card-header">
			 <%= event.content%>
			 <div class="picture-edit">
					 <%= link_to '詳細', event_path(event.id),{class:"btn btn-primary"} %>
			 </div>
		 </div>
		 <% end %>
   </div>
</body>
</html>

<style>
.modal-content{
	height:150%;
	width:150%;
}

.user_info{
  border-radius: 50%;
  border: inset 4px #ff0000;
}
</style>
